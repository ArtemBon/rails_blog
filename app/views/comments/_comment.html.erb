<p>
  <strong>Commenter:</strong>
  <%= link_to comment.user.name, user_path(comment.user) %>, <%= image_tag comment.user.display_avatar if comment.user.avatar.attached? %>
</p>

<p>
  <strong>Comment:</strong>
  <%= comment.body %>
</p>

<p>
  <%= link_to 'Destroy Comment', [comment.article, comment], method: :delete, data: { confirm: 'Are you sure?' } if current_user?(comment.article.user) || current_user?(comment.user) %>
</p>
